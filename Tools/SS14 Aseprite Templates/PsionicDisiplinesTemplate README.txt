The locked overlay layer is a general color mask to convert and unlocked icon to a locked one already setup. It needs to be cut to the sprites
shape as to leave the background transparent.
The mask template layer is as the name susggests, a mask, to produce anything that should go under the disk if it is in
the center of the disk. Use it to cut from the sprite into a new layer and order it below the disk.
